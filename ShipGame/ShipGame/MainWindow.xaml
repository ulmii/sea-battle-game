<Window x:Class="ShipGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShipGame"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" MinHeight="720" MinWidth="1000" Width="1000">

    <Window.Resources>
        <DataTemplate x:Key="DataTemplate_Level2">
            <Button x:Name="ItemButton" Click="Turn" DataContext="{Binding}"  Content="{Binding Content, UpdateSourceTrigger=PropertyChanged}" Height="30" Width="30" Margin="1,1,1,1"/>
        </DataTemplate>

        <DataTemplate x:Key="DataTemplate_Level1">
            <ItemsControl ItemsSource="{Binding}" ItemTemplate="{DynamicResource DataTemplate_Level2}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </DataTemplate>

    </Window.Resources>

    <Grid>
        <Viewbox x:Name="PlayerHitsVB" HorizontalAlignment="Left"  Width="Auto" MaxWidth="400" Height="Auto" Margin="50" VerticalAlignment="Center" Grid.Column="2">
            <ItemsControl x:Name="PlayerHitsIS" ItemTemplate="{DynamicResource DataTemplate_Level1}"/>
        </Viewbox>
        <Viewbox x:Name="PlayerShipsVB" HorizontalAlignment="{Binding}"  Width="Auto" MaxWidth="400" Height="Auto" Margin="50" VerticalAlignment="Center" Grid.Column="2">
            <ItemsControl x:Name="PlayerShipsIS" ItemTemplate="{DynamicResource DataTemplate_Level1}"/>
        </Viewbox>
        <Label x:Name="PlayerShipSizeLabel" Content="Add ship of size:" HorizontalAlignment="Center" Margin="0, 0, 50, 0" VerticalAlignment="Top" Height="Auto" Width="Auto" FontSize="50"/>
        <Label x:Name="PlayerShipSize" Content="1" HorizontalAlignment="Center"  Margin="350, 0, 0, 0" VerticalAlignment="Top" Height="Auto" Width="Auto" FontSize="50"/>
        <Label x:Name="GameEndedLabel" Content="" HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="193" Width="809" FontSize="72" Visibility="Hidden"/>
        <Label x:Name="LegendLabel" Content="* - Ship&#10;O - Miss&#10;X - Hit" HorizontalAlignment="Left" Margin="20" VerticalAlignment="Bottom" Height="Auto" Width="Auto" FontSize="24"/>
    </Grid>
</Window>
